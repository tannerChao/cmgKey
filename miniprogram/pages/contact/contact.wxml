<!--logs.wxml-->
<view class="container contact-us">
     <view class="contact-us-content theme-background">
        <view class="content">
            <text class="phone">132-1126-3025</text>
            <text class="address">广东省江门市蓬江区杜阮镇长乔凤山工业区10号</text>
            <text class="email">1366494128@qq.com</text>
            <text class="email">https://shop3950qq7m59485.1688.com</text>
        </view> 
     </view>
     <view class="page-section page-section-gap">
        <map
          subkey="{{subKey}}"
          style="width: 100%; height: 300px;"
          latitude="{{latitude}}"
          longitude="{{longitude}}"
          scale="{{18}}"
          markers="{{markers}}"
          covers="{{covers}}"
          enable-3D="{{enable3d}}"
          show-compass="{{showCompass}}"
          enable-zoom="{{enableZoom}}"
          enable-rotate="{{enableRotate}}"
          enable-overlooking="{{enableOverlooking}}"
          enable-scroll="{{enableScroll}}"
          polygons="{{drawPolygon ? polygons : []}}"
        >
        </map>
      </view>
      <phone></phone>
</view>